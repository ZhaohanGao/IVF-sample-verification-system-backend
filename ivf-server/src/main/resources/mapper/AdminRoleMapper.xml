<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nenu.server.mapper.AdminRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.nenu.server.pojo.AdminRole">
        <id column="id" property="id" />
        <result column="adminId" property="adminId" />
        <result column="rId" property="rId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, adminId, rId
    </sql>

    <!-- 更新操作员角色 -->
    <update id="addAdminRole">
        INSERT INTO t_admin_role(adminId,rid) values 
        <foreach collection="rids" item="rid" separator=",">
            (#{adminId},#{rid})
        </foreach>
    </update>

    <!-- 为新添加的患者类型操作员更新角色 -->
    <insert id="setNewAdminRole">
        INSERT INTO t_admin_role (t_admin_role.adminId,t_admin_role.rId) VALUES (#{id},1)
    </insert>
</mapper>
